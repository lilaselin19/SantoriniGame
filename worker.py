class Worker:    def __init__(self, name, myPlayer, x, y):        self._name = name        self._myPlayer = myPlayer        self._x = x        self._y = y        self._level = 0    def move(self, direction):        # what pattern can be used instead of giant if else chain?        # ie to replace            # if direction == "n":                #y = y - 1        raise NotImplementedError    def build(self, direction):        # same thing as build        raise NotImplementedError